#include <iostream>
#include <vector>

void quick_sort(std::vector<int> list) {
    if (list.size() == 0)
    {
        return list[0];
    }
    

}

int main() {

    std::vector<int> list = {};
    quick_sort(list);


}