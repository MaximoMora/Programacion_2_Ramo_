#include <iostream>
#include <vector>


void merge_sort(std::vector<int> list) {
    int len = list.size();
    if (list.size() == 0) {
        return list[0]
    } 

    left_sorted = merge_sort(len)
    right_sorted = merge_sort()
    
}

int main() {


}